generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model cities {
  city       String
  state_id   String
  state_name String
  lat        Float  @db.Real
  lng        Float  @db.Real
  population Int
  id         String @id
}
